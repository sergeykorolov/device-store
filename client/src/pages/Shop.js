import React from 'react';
import TypeBar from "../components/TypeBar";

const Shop = () => {
    return (
        <div className="shop container">
            <TypeBar/>
        </div>
    );
};

export default Shop;